# action.yml
name: 'Katalon-Studio'
description: 'KS Action for running test'
inputs:
  ks_command:
    description: 'katalon parameter'
    required: true
outputs:
  status:
      description: 'Status of running KS Test'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.ks_command}}